#include <stdio.h>
#include <conio.h>

int main()
{
     int i=10;
     printf("first 10 odd number in reverse order \n");
    while (i>=1 && i<=10)
    {
      printf("%d\n",i*2-1);
      i--;
    }
    getch();

}
